import { IconButton, styled } from "@mui/material";

export const StyledCalendarHeaderRoot = styled("div")`
  display: flex;
  justify-content: space-between;
  padding-block: 4px 8px;
  align-items: center;
`;

export const StyledIconButton = styled(IconButton)`
  padding: 12px;
`;
